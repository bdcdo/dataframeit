name: Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.13']
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Verify Codex remains optional
        run: >-
          uv run --python "${{ matrix.python-version }}" --extra dev python -c
          'from importlib.metadata import distributions;
          installed = {d.metadata["Name"].lower() for d in distributions()};
          packages = {"filelock", "openai-codex",
          "openai-codex-cli-bin"};
          assert not packages & installed'

      - name: Lint
        run: uv run --python "${{ matrix.python-version }}" --extra dev ruff check src tests

      - name: Run tests
        run: uv run --python "${{ matrix.python-version }}" --extra dev --extra search-all pytest

  # Instala cada dependência direta na menor versão que o pyproject.toml aceita.
  # Um piso que não resolve, ou que resolve para uma combinação quebrada, falha
  # aqui. Roda em 3.10 porque os pisos de pandas e pyarrow não têm wheel para
  # 3.13, onde o instalador escolhe versões mais novas.
  lowest-direct:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Run tests (base)
        run: uv run --python 3.10 --resolution lowest-direct --extra dev pytest

      - name: Run tests (extras)
        run: >-
          uv run --python 3.10 --resolution lowest-direct --extra dev
          --extra all --extra excel --extra polars --extra search-all pytest

  codex-provider:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.13']
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Run tests with the Codex SDK and bundled runtime
        run: >-
          uv run --python "${{ matrix.python-version }}"
          --extra dev --extra codex pytest

  codex-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Verify bundled runtime and portable authentication lifecycle
        run: >-
          uv run --python 3.13 --extra dev --extra codex pytest
          tests/test_codex.py::TestBackendLifecycle
          tests/test_codex_runtime.py

  docs:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Build documentation
        run: uv run --python 3.13 --extra docs mkdocs build
